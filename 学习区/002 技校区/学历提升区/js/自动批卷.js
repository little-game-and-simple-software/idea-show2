function choose(self)
{
    var p_value = self.value
    var father = self.parentNode
}
$(function()
{
    function commit()
    {
        $(".selection > input[type='radio']").on("click",function()
        {
            console.log($(this).val());
        })
    }

    function read(){}

    function result(){}
})